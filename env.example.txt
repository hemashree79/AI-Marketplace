# Copy this file's contents into your host's "Environment Variables" settings.
# Do NOT commit an actual .env file with real secrets to git.

SECRET_KEY=replace-with-a-long-random-string
ADMIN_EMAIL=admin@yourdomain.com
ADMIN_PASSWORD=choose-a-strong-password
FLASK_DEBUG=false